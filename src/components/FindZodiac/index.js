import React from "react";

function ZodiacSearch() {
const findSign = () => {
    return (
        <div>
          <h1 className="horoscope">HOROSCOPE</h1>
          <p>
            Don’t know your zodiac? Find it from a database by inputting birthdate
          </p>
          {/* insert code to select date from drop down and return a zodiac sign. Maybe use switch case */}
        </div>
      );
};
}

export default ZodiacSearch;